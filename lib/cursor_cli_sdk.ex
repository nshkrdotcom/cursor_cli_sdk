defmodule CursorCliSdk do
  @moduledoc """
  Barebones Elixir SDK namespace for the Cursor Agent CLI.

  Provider implementation is intentionally deferred to the Cursor CLI
  integration docsets. This package currently establishes the application,
  documentation, branding, and Hex package boundary.
  """
end
